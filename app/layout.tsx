import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'Next Fortress - Enterprise Next.js Template',
  description:
    'Production-ready Next.js template with TypeScript, testing, and CI/CD',
  keywords: ['nextjs', 'typescript', 'react', 'template', 'tailwindcss'],
  authors: [{ name: '33SLueck', url: 'https://github.com/33SLueck' }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
